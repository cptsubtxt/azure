name: Simple Usage

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-22.04
    steps:
      - uses: docker://pandoc/extra
        with:
          args: >-  # allows you to break string into multiple lines
            --template eisvogel 
            --listings
            --standalone
            --output=example.pdf
            example.md
